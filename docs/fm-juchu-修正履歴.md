# 受注入力（FileMaker Web版）修正履歴

本多さんからの修正依頼と、その対応を1件ずつ残す記録簿です。
最終的に案件管理（Hub本体の案件入力）へ反映するときの根拠になります。

- 対象ファイル: `tools/fm-juchu.html`
- 生成元: `C:\Users\web\archive-tools\build_fm_juchu6.py` ＋ `scratchpad/keep.js`
  （**HTMLを直接編集せず、生成スクリプト経由で作り直す**）
- 付け合わせ・判断の記録: `tools/fm-compare.html` の〔変更記録〕〔反映指示書を書き出す〕

---

## 依頼と対応

| # | 日付 | 本多さんの依頼（原文） | 対応 | 版 |
|---|---|---|---|---|
| 1 | 07-31 | FileMakerと全く同じものをHub上に作りたい | 受注入力をWeb版として新規作成 | — |
| 2 | 07-31 | どの項目が使われているか知りたい | 使われ方レポート（入力率・修正回数・所要時間）を追加。氏名・端末名は記録しない | — |
| 3 | 08-01 | 全然受注登録風ではないけど／みためがぜんぜんちがう | 「表が主役」のFileMaker構造に作り直し（第2〜5版） | — |
| 4 | 08-01 | 表AとBの違いがわからない。もう少し全体的に大きく | 用紙／印刷／加工の切替に修正し、全体を拡大 | — |
| 5 | 08-01 | 単価設定・印刷カレンダー・原価比較・登録ボタンは不要。位置も合わせて | 4ブロックを削除。ヘッダを実物の配置に再構成し、7行すべて折り返しゼロに | v499 |
| 6 | 08-01 | （計算式パネルの写真）不要 | 計算式パネルを撤去。式は生成スクリプトに保存 | v500 |
| 7 | 08-01 | ここの表記がうまくいってない（下部が左にずれる） | 下部がシートの外にあり全幅化していたのを修正。外注先が潰れて縦折れしていた列幅も再配分 | v501 |
| 8 | 08-01 | デザインとしては、tokiwahubに合った感じにして | techC（黒ヘッダー・生成り背景・白カード・黒メトリクス）へ全面刷新。見積業者を1業者1行の表に | v502 |
| 9 | 08-01 | 不要な項目・過去データから不要そうな項目を色付けで分かりやすく | 過去2年11,781件の入力実績で色分け（赤=0件／橙=1%未満／黄=1〜10%／灰=照合不可） | v503 |
| 10 | 08-01 | （2024・2025のFM実績を）過去実績として入力して | 実績JSONを作成し、📁取込→検索→リピート起票を実装（IndexedDB） | v504 |
| 11 | 08-01 | もう少し横画面いっぱいでいい | 固定1460pxをやめ可変幅に。明細表 776→1183px | v505 |
| 12 | 08-01 | もう少し黒の濃度を強くして | 黒・罫線・文字色を一段濃く（シニア向け高コントラスト） | v506 |
| 13 | 08-01 | 検索モードができない／パーツ数・表紙有無はHubの機能を追加 | 検索モードを実装（部分一致・複数欄AND）。09の下を「表紙」に直し、パーツ数と表紙で明細行を出し分け | v507 |
| 14 | 08-01 | 上記の入力画面でいいと思うので、いらないかな（下部の重複欄） | 紙質/規格/色数/切数/面付/横ミシン/インキを撤去。ホットフォルダは残し印刷タブ連動に | v508 |
| 15 | 08-01 | 角丸でいい。右側の見積業者はいらない | 見積業者1〜5を撤去（過去2年で全件同じ値＝未使用）。明細表を全幅に | v509 |
| 16 | 08-01 | タブではなく、はじめから全表示するように | 用紙・印刷・加工のタブを廃止し3表を常時表示 | v510 |
| 17 | 08-01 | 見積行は要らない。その上は右側一杯に作って行の幅をちぢめて | 見積行を撤去。ヘッダの幅制限を外し、行間3→1px・入力欄23→21pxに圧縮 | v511 |
| 18 | 08-01 | すべてレイアウト編集で移動できるように／文字も変更できるように／幅も変更できるように | レイアウト編集を追加（並べ替え28か所・項目名の書き換え・幅のドラッグ変更）。端末に保存、元に戻す付き | v512 |
| 19 | 08-01 | 右側一杯へ、外注のところいらない | 外注先1〜4を撤去（FMの書き出しに列が無い）。下部を右端まで使用 | v513 |
| 20 | 08-04 | 実績を見ながら作りたい／横ボタンで前後移動（別セッション経由） | 〔実績を見ながら〕で得意先・製品名の近い過去実績を一覧表示。◀前/次▶のレコード送り | v532 |
| 21 | 08-05 | ファイルメーカー風の方に色付けてください。現在のファイルメーカーで使用している、していない。頻度等で | 色分けを5段階に（よく使う50%以上／ときどき10〜50%／たまに1〜10%／ほとんど無し1%未満／2年間0件／照合不可）。明細01〜09行も行ごとの実績で塗り分け、実績を紐付けた項目を74→308に拡張 | v544 |
| 22 | 08-05 | 色が付かない | **不具合**: #21でCSSの置換範囲を取り違え、`.refbox`/`.laybar`/`.findbar`/`.ulegend` の「ふだんは隠す」指定ごと消していた。色が付かず、実績パネル・レイアウト編集バー・検索バーが常時表示されていた。消えた指定を復旧 | v545 |

---

## 記録のしかた（今後）

1. 依頼を受けたら、この表に **原文のまま** 1行足す
2. 対応内容と版（SWのバージョン）を記入
3. 実測で確認した内容はコミットメッセージに残す
4. 案件管理へ反映すべきものは `tools/fm-compare.html` の〔決定〕で「Hubに足す」を選び、メモに理由を書く
   → 〔反映指示書を書き出す〕にそのまま載る

---

## わかっている数字（判断の材料）

- 画面の項目の使われ方（2026-08-05・5段階）: よく使う **59** ／ ときどき **17** ／ たまに **75** ／ ほとんど無し **99** ／ 2年間0件 **29** ／ 照合不可 **9**
  - 用紙明細は 1行目=よく使う・2〜3行目=ときどき・4〜5行目=たまに・8行目以降=ほとんど無し。**4行目以降はほぼ使われていない**
- FileMaker受注入力の項目: **106**（うちHubの案件入力に無い: **86**、実績10%以上のもの: **33**）
- 過去2年の実績: **11,781件**（2024年 5,095 / 2025年 6,686）
- FMの全809列のうち、**2年間1件も入力が無い列が185、全件同じ値の列が179**。
  1件ずつ人が入れている項目は全体の2割程度
- **伝票番号と no（納品書No.）が違う件数**（2026-08-05 実データ集計）
  - 全体: 11,780件中 **636件（5.40%）**
  - トッパン（ﾄｯﾊﾟﾝﾌｫｰﾑｽﾞ㈱浜松）: 582件中 24件（4.12%）
  - **トッパン以外: 11,198件中 612件（5.47%）**
  - 不一致の99%が枝番付き（例 `a090903-A` ↔ `a090904`）＝1受注を分割納品し、伝票番号に-A/-B、納品書は別連番
  - 多い得意先: ㈱池田屋商店94／内外情勢調査会82／浜松医科大学63／祭すみたや49／鈴木半商事25
  - → **トッパン固有ではなく全社的な運用**。no は独立項目として必要
